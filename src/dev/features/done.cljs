(ns dev.features.done
  (:require [dev.util :refer-macros [defeditor]]))

(defeditor
 TODO
 "This is should become the style guide of all the current features"
 [:document
  [:header1 "Add some features"]])
